<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Update <%= user.first %> Info</title>
</head>
<body>
  <%- include('../partials/header') %>
  <div class="card" style="max-width:450px; margin:2.5rem auto; padding:2rem;">
    <h2 style="text-align:center; margin-bottom:1.5rem;">✏️ Update Your Info</h2>
    <form action="/users/<%= user._id %>?_method=PUT" method="POST" style="display:flex; flex-direction:column; gap:1.1rem;">
      <div>
        <label for="first" style="font-weight:500;">First Name:</label>
        <input type="text" id="first" name="first" value="<%= user.first %>" required autocomplete="given-name" style="margin-top:0.3rem;">
      </div>
      <div>
        <label for="last" style="font-weight:500;">Last Name:</label>
        <input type="text" id="last" name="last" value="<%= user.last %>" required autocomplete="family-name" style="margin-top:0.3rem;">
      </div>
      <div>
        <label for="email" style="font-weight:500;">Email:</label>
        <input type="email" id="email" name="email" value="<%= user.email %>" required autocomplete="email" style="margin-top:0.3rem;">
      </div>
      <div>
        <label for="address" style="font-weight:500;">Address:</label>
        <input type="text" id="address" name="address" value="<%= user.address %>" required autocomplete="street-address" style="margin-top:0.3rem;">
      </div>
      <div>
        <label for="phone" style="font-weight:500;">Phone:</label>
        <input type="text" id="phone" name="phone" value="<%= user.phone %>" required autocomplete="tel" style="margin-top:0.3rem;">
      </div>
      <button type="submit" class="btn btn-success" style="margin-top:0.7rem;">✅ Confirm Update</button>
      <div style="text-align:center; margin-top:1rem;">
        <a href="/users/<%= user._id %>" class="btn btn-primary">🔙 Back to Profile</a>
      </div>
    </form>
  </div>
  <%- include('../partials/footer') %>
</body>
</html>
